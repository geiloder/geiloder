# Foundation — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vollständiges Next.js 14 Projekt mit TypeScript, Tailwind, Supabase-Verbindung und Vercel-Deployment — lauffähig bevor ein einziger Feature-Code geschrieben wird.

**Architecture:** Next.js 14 App Router, TypeScript strict mode, Tailwind CSS für Styling, shadcn/ui für Admin-Komponenten, Supabase für DB + Storage + Auth, Vercel für Hosting. Alle Secrets in `.env.local` (lokal) und GitHub Secrets (CI/CD).

**Tech Stack:** Next.js 14, TypeScript, Tailwind CSS, shadcn/ui, @supabase/supabase-js, Playwright, @google/generative-ai

---

## Voraussetzungen (du machst das manuell)

- [ ] GitHub Account erstellen / einloggen
- [ ] Neues Repo anlegen: `geiloder` (public oder private, beides ok)
- [ ] Supabase Account erstellen: https://supabase.com
- [ ] Supabase Projekt erstellen: Name `geiloder`, Region `eu-central-1` (Frankfurt)
- [ ] Vercel Account erstellen: https://vercel.com (mit GitHub verbinden)
- [ ] Google AI Studio API Key holen: https://aistudio.google.com → "Get API key" → "Create API key" → Key kopieren

---

## File Structure (wird in diesem Plan erstellt)

```
/
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   └── globals.css
│   ├── lib/
│   │   ├── supabase/
│   │   │   ├── client.ts
│   │   │   └── server.ts
│   │   └── utils.ts
│   └── types/
│       └── index.ts
├── .env.local.example
├── .gitignore
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.ts
```

---

### Task 1: Git Repository initialisieren

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: In Projektverzeichnis wechseln und Git initialisieren**

```bash
cd /Users/daniel/Projekte/geiloder
git init
git remote add origin https://github.com/DEIN_USERNAME/geiloder.git
```

- [ ] **Step 2: .gitignore erstellen**

```
# dependencies
node_modules/
.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# rendered assets (committed via GitHub Actions, not local)
/rendered/

# ffmpeg output
/videos/
```

- [ ] **Step 3: Ersten Commit erstellen**

```bash
git add .gitignore
git commit -m "chore: initialize repo"
```

---

### Task 2: Next.js Projekt erstellen

**Files:**
- Create: `package.json`, `tsconfig.json`, `tailwind.config.ts`, `next.config.ts`, `src/app/globals.css`, `src/app/layout.tsx`, `src/app/page.tsx`

- [ ] **Step 1: Next.js erstellen**

```bash
cd /Users/daniel/Projekte/geiloder
npx create-next-app@14 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-git
```

Wenn gefragt, alle Defaults akzeptieren außer `--no-git` (wir haben bereits ein Repo).

- [ ] **Step 2: Zusätzliche Dependencies installieren**

```bash
npm install @supabase/supabase-js @supabase/ssr @google/generative-ai
npm install -D playwright @playwright/test
npx playwright install chromium
```

- [ ] **Step 3: shadcn/ui initialisieren**

```bash
npx shadcn@latest init
```

Wenn gefragt:
- Style: **Default**
- Base color: **Zinc**
- CSS variables: **Yes**

- [ ] **Step 4: shadcn Basiskomponenten installieren**

```bash
npx shadcn@latest add button card badge table input label textarea select dialog sheet
```

- [ ] **Step 5: tsconfig.json prüfen — strict mode muss an sein**

`tsconfig.json` öffnen und sicherstellen:

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true
  }
}
```

- [ ] **Step 6: next.config.ts anpassen (Produktbilder von externen Domains erlauben)**

```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.awin.com' },
      { protocol: 'https', hostname: '**.adcell.de' },
      { protocol: 'https', hostname: '**.amazon.de' },
      { protocol: 'https', hostname: '**.amazon.com' },
      { protocol: 'https', hostname: '**.supabase.co' },
      { protocol: 'https', hostname: '**.cdninstagram.com' },
    ],
  },
}

export default nextConfig
```

- [ ] **Step 7: Root Layout erstellen**

`src/app/layout.tsx`:

```typescript
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: {
    default: 'Geil oder? — Täglich Deals, die wirklich geil sind',
    template: '%s | Geil oder?',
  },
  description: 'Täglich kuratierte Deals aus Fitness, Supplements, Home Gym und mehr. Geil oder?',
  metadataBase: new URL(
    process.env.NEXT_PUBLIC_SITE_URL ?? 'https://geiloder.vercel.app'
  ),
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="de">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
```

- [ ] **Step 8: Placeholder Home Page**

`src/app/page.tsx`:

```typescript
export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold">Geil oder?</h1>
        <p className="mt-2 text-gray-500">Täglich Deals, die wirklich geil sind.</p>
      </div>
    </main>
  )
}
```

- [ ] **Step 9: Dev-Server starten und prüfen**

```bash
npm run dev
```

Expected: Browser öffnet http://localhost:3000 → zeigt "Geil oder?" Text.

---

### Task 3: TypeScript Types definieren

**Files:**
- Create: `src/types/index.ts`

- [ ] **Step 1: Alle Domain-Types anlegen**

`src/types/index.ts`:

```typescript
export type DealStatus =
  | 'new'
  | 'approved'
  | 'rejected'
  | 'rendered'
  | 'scheduled'
  | 'posted'
  | 'expired'

export type DealKategorie =
  | 'supplements'
  | 'fitness'
  | 'home-gym'
  | 'gymwear'
  | 'gadgets'
  | 'beauty'
  | 'home-living'
  | 'tech'
  | 'fashion'
  | 'sonstige'

export type DealQuelle = 'awin' | 'adcell' | 'amazon' | 'manuell'

export interface Deal {
  id: string
  external_id: string | null
  quelle: DealQuelle
  produktname: string
  marke: string | null
  shop: string
  kategorie: DealKategorie
  alter_preis: number | null
  deal_preis: number
  rabatt_prozent: number | null
  gutschein_code: string | null
  verfuegbarkeit: string | null
  produktbild_url: string | null
  affiliate_link: string
  landingpage_url: string | null
  provision: number | null
  deal_score: number | null
  status: DealStatus
  copy_data: DealCopy | null
  slug: string | null
  created_at: string
  expires_at: string | null
  posted_at: string | null
  updated_at: string
}

export interface DealCopy {
  headline: string
  subheadline: string
  benefit_1: string
  benefit_1_detail: string
  benefit_2: string
  benefit_2_detail: string
  benefit_3: string
  benefit_3_detail: string
  cta: string
  caption: string
  hashtags: string[]
  humor_intro: string
  humor_konsequenz_1: string
  humor_konsequenz_2: string
  humor_konsequenz_3: string
  humor_cta: string
}

export interface Shop {
  id: string
  name: string
  domain: string | null
  affiliate_programm: string | null
  social_erlaubt: boolean
  deeplink_erlaubt: boolean
  bilder_erlaubt: boolean
  provision_rate: number | null
  aktiv: boolean
  created_at: string
}

export interface Post {
  id: string
  deal_id: string
  plattform: 'instagram' | 'tiktok' | 'pinterest' | 'youtube'
  post_type: 'carousel' | 'story' | 'reel' | 'pin'
  status: 'pending' | 'scheduled' | 'posted' | 'failed'
  scheduled_for: string | null
  posted_at: string | null
  external_post_id: string | null
  assets: PostAssets | null
  created_at: string
}

export interface PostAssets {
  slides?: string[]    // Supabase Storage URLs
  story?: string[]
  video?: string | null
  thumbnail?: string | null
}

export interface Click {
  id: string
  deal_id: string
  source: string | null
  plattform: string | null
  post_id: string | null
  ip_hash: string | null
  user_agent: string | null
  utm_campaign: string | null
  utm_content: string | null
  created_at: string
}

export interface RawFeedDeal {
  external_id: string
  quelle: DealQuelle
  produktname: string
  marke?: string
  shop: string
  alter_preis?: number
  deal_preis: number
  produktbild_url?: string
  affiliate_link: string
  landingpage_url?: string
  provision?: number
  verfuegbarkeit?: string
  gutschein_code?: string
  expires_at?: string
}
```

- [ ] **Step 2: Type-Check ausführen**

```bash
npx tsc --noEmit
```

Expected: Keine Fehler.

---

### Task 4: Supabase Client einrichten

**Files:**
- Create: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`

- [ ] **Step 1: .env.local.example erstellen**

```bash
cat > .env.local.example << 'EOF'
# Supabase (aus Supabase Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://DEIN_PROJEKT_ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=DEIN_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=DEIN_SERVICE_ROLE_KEY

# Gemini API (aus Google AI Studio)
GEMINI_API_KEY=DEIN_GEMINI_KEY

# Site
NEXT_PUBLIC_SITE_URL=https://geiloder.vercel.app

# Admin
ADMIN_PASSWORD=SICHERES_PASSWORT_HIER
EOF
```

- [ ] **Step 2: .env.local erstellen (mit echten Werten)**

```bash
cp .env.local.example .env.local
# Dann .env.local öffnen und echte Werte eintragen
```

Die Werte findest du in:
- Supabase: Dashboard → Project → Settings → API
- Google AI Studio: https://aistudio.google.com

- [ ] **Step 3: Supabase Browser Client**

`src/lib/supabase/client.ts`:

```typescript
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

- [ ] **Step 4: Supabase Server Client**

`src/lib/supabase/server.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {}
        },
      },
    }
  )
}

export function createServiceClient() {
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { cookies: { getAll: () => [], setAll: () => {} } }
  )
}
```

- [ ] **Step 5: Verbindung testen**

`src/app/api/health/route.ts` erstellen:

```typescript
import { createServiceClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function GET() {
  try {
    const supabase = createServiceClient()
    const { error } = await supabase.from('_health').select('*').limit(1)
    // Tabelle existiert noch nicht, aber connection error wäre anders
    return NextResponse.json({ status: 'ok', supabase: !error || error.code === 'PGRST116' ? 'connected' : 'error' })
  } catch (e) {
    return NextResponse.json({ status: 'error', message: String(e) }, { status: 500 })
  }
}
```

```bash
npm run dev
# In neuem Terminal:
curl http://localhost:3000/api/health
```

Expected: `{"status":"ok","supabase":"connected"}`

---

### Task 5: Utils einrichten

**Files:**
- Create: `src/lib/utils.ts`

- [ ] **Step 1: Utility-Funktionen**

`src/lib/utils.ts`:

```typescript
import { type ClassValue, clsx } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

export function formatPrice(price: number): string {
  return new Intl.NumberFormat('de-DE', {
    style: 'currency',
    currency: 'EUR',
  }).format(price)
}

export function formatDiscount(percent: number): string {
  return `-${Math.round(percent)}%`
}

export function slugify(text: string): string {
  return text
    .toLowerCase()
    .replace(/ä/g, 'ae')
    .replace(/ö/g, 'oe')
    .replace(/ü/g, 'ue')
    .replace(/ß/g, 'ss')
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
}

export function generateDealSlug(produktname: string, dealId: string): string {
  return `${slugify(produktname)}-${dealId.slice(0, 8)}`
}

export function hashIp(ip: string): string {
  // Simple hash for privacy — not cryptographic
  let hash = 0
  for (let i = 0; i < ip.length; i++) {
    const char = ip.charCodeAt(i)
    hash = ((hash << 5) - hash) + char
    hash = hash & hash
  }
  return Math.abs(hash).toString(36)
}
```

- [ ] **Step 2: clsx + tailwind-merge installieren (falls nicht von shadcn installiert)**

```bash
npm install clsx tailwind-merge
```

- [ ] **Step 3: Type-Check**

```bash
npx tsc --noEmit
```

Expected: Keine Fehler.

---

### Task 6: Vercel Deployment

**Files:**
- Modify: nichts — nur Vercel konfigurieren

- [ ] **Step 1: Code committen und pushen**

```bash
git add -A
git commit -m "feat: initial Next.js project with Supabase + TypeScript setup"
git push -u origin main
```

- [ ] **Step 2: Vercel Projekt verbinden**

1. https://vercel.com/new
2. "Import Git Repository" → `geiloder` auswählen
3. Framework: **Next.js** (auto-detected)
4. Root directory: `.` (leer lassen)
5. Environment Variables eintragen:
   - `NEXT_PUBLIC_SUPABASE_URL` = dein Wert
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = dein Wert
   - `SUPABASE_SERVICE_ROLE_KEY` = dein Wert
   - `GEMINI_API_KEY` = dein Wert
   - `NEXT_PUBLIC_SITE_URL` = `https://geiloder.vercel.app` (oder deine Vercel URL)
   - `ADMIN_PASSWORD` = dein Passwort
6. Deploy klicken

- [ ] **Step 3: Deployment prüfen**

```bash
curl https://geiloder.vercel.app
```

Expected: HTML-Response mit "Geil oder?" Text.

- [ ] **Step 4: Health-Check auf Vercel prüfen**

```bash
curl https://geiloder.vercel.app/api/health
```

Expected: `{"status":"ok","supabase":"connected"}`

---

### Task 7: GitHub Secrets einrichten (für spätere GitHub Actions)

- [ ] **Step 1: GitHub Repo → Settings → Secrets and variables → Actions**

Folgende Secrets hinzufügen:

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
GEMINI_API_KEY
ADMIN_PASSWORD
```

- [ ] **Step 2: Secrets prüfen**

In GitHub → Settings → Secrets: alle 5 Secrets sollen sichtbar sein (Werte sind versteckt).

---

**Plan abgeschlossen wenn:**
- `npm run dev` läuft ohne Fehler
- `npx tsc --noEmit` gibt 0 Fehler
- Vercel Deployment ist grün
- `/api/health` gibt `{"status":"ok"}` zurück
- GitHub Secrets sind gesetzt
